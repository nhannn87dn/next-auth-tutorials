/**
 * Call API
 * @returns 
 */
export const verifyToken = async () => {
    return  {
        name: "Alex",
        token: "token admin",
        role: "admin",
        isAuthenticated: true
    }
}